{
  "tasks": {
    "build": "wasm-pack build --release --target deno",
    "dev": "deno run --allow-read --watch main.ts",
    "test": "deno test --allow-read"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}